node_modules/
# package-lock.json is committed on purpose: CI runs npm ci, and a validator whose
# dependency versions drift is a validator whose results are not reproducible.
.DS_Store
Thumbs.db
*.log
.env
.env.*
.idea/
.vscode/

# Captured decision-record stores hold prompt and tool text — personal data. Never commit them.
.glassbox/
